version: v1.0
name: Provisioning pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: "Test"
    task:
      jobs:
      - name: Test provisioning in docker container
        commands:
          - checkout
          - docker build --file test/Dockerfile .
